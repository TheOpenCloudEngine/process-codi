<%
	if(value){
		var session = mw3.getAutowiredObject('org.uengine.codi.mw3.model.Session');
		
		var admin = false;
		if(session && session.employee)
			admin = session.employee.isAdmin;
		
		if(value.processMapList){
%>



	<div class="guide">
		<span class="tl"></span>
		<span class="tr"></span>
	</div>
	<div class="cont" style="padding:10px;">
		<ul class="process_map2">		
						
<%
			for (var i=0; i<value.processMapList.length; i++){
				if(typeof options == 'undefined') 
					options = objectMetadata;
			
				options['index'] = i;
				options['htmlTag'] =  'li';
%>
						<%=mw3.locateObject(value.processMapList[i], value.processMapList[i].__className, null, options, {objectId: value.__objectId, name: 'processMapList[' + i + ']'})%>	
<%
			}
%>
		</ul>
<%          
			if(true){
%>					<br><br>
		<%=methods.addApp.here()%>
<%
			 }
%>					
		<div class="clear"></div>
		<% if( value.dueDateSetting ){ %>
		<div><span style="color: red">* <%=mw3.localize("$existingSchedule")%></span></div>
		<% } %>
	</div>

	<div class="guide">
		<span class="lb"></span>
		<span class="rb"></span>
	</div>



<%
		}
	}
%>	